import { FC } from 'react';

import { Batching } from '@react-18/components/Batching';

const Components = {
  Batching,
};

export const App: FC = () => (
  <div>
    <Components.Batching />
  </div>
);
